import 'package:flutter/material.dart';

class AppRadius {
  static const sm = Radius.circular(8);
  static const md = Radius.circular(12);
  static const lg = Radius.circular(16);
}

class AppElevation {
  static const card = 2.0;
  static const modal = 4.0;
}
